import { BrowserRouter } from 'react-router-dom'
import { AppShell } from '@/app/app-shell'

function App() {
  return (
    <BrowserRouter>
      <AppShell />
    </BrowserRouter>
  )
}

export default App
